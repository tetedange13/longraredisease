params {
    modules_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/'
}

process {
    withName: 'DOWNSAMPLE_SV' {
        ext.prefix = { "${meta.id}.${meta.caller}.downsampled" }
        ext.suffix = 'covFiltered'
    }
}
